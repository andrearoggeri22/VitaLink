# Configurazione database
DATABASE_URL=postgresql://vitalink:vitalink_password@db:5432/vitalink
PGHOST=db
PGPORT=5432
PGUSER=vitalink
PGPASSWORD=vitalink_password
PGDATABASE=vitalink

# Configurazione Flask
FLASK_APP=main.py
FLASK_ENV=production
FLASK_DEBUG=0
PYTHONUNBUFFERED=1

# Configurazione JWT
JWT_SECRET_KEY=cambia_questo_con_una_chiave_segreta_sicura

# Configurazione Twilio (per notifiche SMS)
TWILIO_ACCOUNT_SID=il_tuo_sid_twilio
TWILIO_AUTH_TOKEN=il_tuo_token_twilio
TWILIO_PHONE_NUMBER=il_tuo_numero_twilio

# Configurazione SendGrid (per email)
SENDGRID_API_KEY=la_tua_api_key_sendgrid
DEFAULT_FROM_EMAIL=no-reply@vitalink.com

# Dati admin predefinito (prima installazione)
ADMIN_EMAIL=admin@vitalink.com
ADMIN_PASSWORD=cambia_questa_password